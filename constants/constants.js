const APP = {
  NAME: 'Dreamt'
};

const DATABASE = {
  NAME: 'dreamt.db'
};

const PATH = {
  DREAM: '/dream'
};

const RATE_LIMIT = {
  IMAGES_PER_DREAM: 3
};

export { APP, DATABASE, PATH, RATE_LIMIT };
